name: CI Lint

on:
  push:
    branches: [ main, develop ]
    paths: [ '**.ps1' ]
  pull_request:
    branches: [ main ]
    paths: [ '**.ps1' ]

jobs:
  powershell-lint:
    name: PSScriptAnalyzer
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      
      - name: Debug - List files
        run: |
          Write-Host "=== Files in repository ==="
          Get-ChildItem -Force
          Write-Host "`n=== Looking for config ==="
          $configExists = Test-Path ".powershell-psscriptanalyzer.psd1"
          Write-Host "Config file exists: $configExists"
          if (-not $configExists) {
            Get-ChildItem -Filter "*.psd1" -Recurse | ForEach-Object { Write-Host "Found: $($_.FullName)" }
          }
      
      - name: Install PSScriptAnalyzer
        run: Install-Module -Name PSScriptAnalyzer -Force -Scope CurrentUser
      
      - name: Analyze
        run: |
          $configPath = ".powershell-psscriptanalyzer.psd1"
          if (Test-Path $configPath) {
            $results = Invoke-ScriptAnalyzer -Path . -Recurse -Settings $configPath
            if ($results) {
              $results | Format-Table
              throw "❌ Found $($results.Count) issues"
            }
            Write-Host "✅ PSScriptAnalyzer: все скрипты в порядке"
          } else {
            Write-Warning "⚠️ Config file not found: $configPath"
            Write-Warning "⚠️ Running without custom settings..."
            $results = Invoke-ScriptAnalyzer -Path . -Recurse
            if ($results) {
              $results | Format-Table
              throw "❌ Found $($results.Count) issues"
            }
          }